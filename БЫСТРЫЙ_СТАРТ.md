# 🚀 Быстрый старт - Всё вместе

## Запуск одной командой

```bash
# Сделайте скрипт исполняемым (один раз)
chmod +x deploy.sh

# Запустите в production режиме (всё вместе)
./deploy.sh prod
```

Готово! Приложение запущено и работает вместе:
- ✅ Frontend + Backend + Database
- ✅ Все сервисы связаны между собой
- ✅ Готово к использованию

## Доступ к приложению

После запуска откройте в браузере:

- **Главная страница**: http://localhost
- **Админ-панель**: http://localhost/admin
- **API**: http://localhost:8000

## Первая настройка

После первого запуска выполните:

```bash
# Создать суперпользователя (админа)
docker-compose -f docker-compose.prod.yml exec backend python manage.py createsuperuser
```

Введите:
- Username: `admin` (или любое другое)
- Email: ваш email
- Password: придумайте пароль

## Что дальше?

1. Откройте http://localhost
2. Зарегистрируйте нового пользователя
3. Войдите в систему
4. Создайте план обучения!

## Остановка

```bash
docker-compose -f docker-compose.prod.yml down
```

## Перезапуск

```bash
docker-compose -f docker-compose.prod.yml restart
```

## Просмотр логов

```bash
docker-compose -f docker-compose.prod.yml logs -f
```

---

**Всё работает вместе!** 🎉

Frontend, Backend и Database объединены и работают как единое приложение.
