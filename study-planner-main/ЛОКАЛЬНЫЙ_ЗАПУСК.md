# 💻 Локальный запуск на Mac

## Вы работаете на Mac, поэтому используйте эти команды:

### 1. Проверьте Docker Desktop

```bash
# Проверьте, что Docker запущен
docker --version
docker-compose --version

# Если Docker не запущен, откройте Docker Desktop из Applications
```

### 2. Запуск приложения локально

```bash
cd /Users/lazizjanasatov/Desktop/study-ai

# Запуск в режиме разработки
docker-compose up --build

# Или используйте скрипт
chmod +x deploy.sh
./deploy.sh dev
```

### 3. Доступ к приложению

- Frontend: http://localhost:3000
- Backend: http://localhost:8000
- Admin: http://localhost:8000/admin

---

## 🌐 Для деплоя на публичный сервер

**Вам нужен отдельный Linux сервер (VPS)!**

Команды из `DEPLOY_PRODUCTION_RU.md` нужно выполнять **на Linux сервере**, а не на Mac.

### Где взять сервер?

1. **DigitalOcean** - https://www.digitalocean.com
2. **Linode** - https://www.linode.com
3. **Vultr** - https://www.vultr.com
4. **AWS EC2** - https://aws.amazon.com
5. **Hetzner** - https://www.hetzner.com

### Что делать:

1. **Купите VPS сервер** (Ubuntu 20.04 или 22.04)
2. **Подключитесь к серверу** через SSH:
   ```bash
   ssh root@IP_ВАШЕГО_СЕРВЕРА
   ```
3. **На сервере выполните команды** из `DEPLOY_PRODUCTION_RU.md`

---

## 📝 Итого:

- **На Mac (локально)**: Используйте `docker-compose up`
- **На Linux сервере (production)**: Используйте команды из `DEPLOY_PRODUCTION_RU.md`
