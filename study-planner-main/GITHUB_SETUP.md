# 🔐 Настройка GitHub для загрузки кода

## Проблема
GitHub больше не поддерживает аутентификацию по паролю. Нужен Personal Access Token (PAT).

## Решение: Создание Personal Access Token

### Шаг 1: Создайте токен на GitHub

1. Перейдите на https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Название токена: `study-ai-deploy` (или любое другое)
4. Выберите срок действия: `90 days` (или `No expiration` для постоянного)
5. Отметьте права (scopes):
   - ✅ `repo` (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **ВАЖНО**: Скопируйте токен сразу! Он показывается только один раз.

### Шаг 2: Используйте токен вместо пароля

При выполнении `git push`:

```bash
git push -u origin main
```

Когда попросит:
- **Username**: ваш GitHub username (например, `LazizjanAsatov`)
- **Password**: вставьте **токен** (не пароль от GitHub!)

## Альтернатива: Использование SSH (рекомендуется)

### Шаг 1: Создайте SSH ключ

```bash
# Проверьте, есть ли уже SSH ключ
ls -al ~/.ssh

# Если нет, создайте новый
ssh-keygen -t ed25519 -C "your_email@example.com"

# Нажмите Enter для всех вопросов (или введите пароль для ключа)
```

### Шаг 2: Добавьте ключ в GitHub

```bash
# Скопируйте публичный ключ
cat ~/.ssh/id_ed25519.pub
```

1. Перейдите на https://github.com/settings/keys
2. Нажмите "New SSH key"
3. Title: `MacBook Air` (или любое название)
4. Key: вставьте скопированный ключ
5. Нажмите "Add SSH key"

### Шаг 3: Измените URL репозитория на SSH

```bash
# Удалите старый remote
git remote remove origin

# Добавьте новый с SSH
git remote add origin git@github.com:LazizjanAsatov/study-planner.git

# Попробуйте загрузить
git push -u origin main
```

## Быстрое решение (через токен)

```bash
# Используйте токен в URL (небезопасно, но работает)
git remote set-url origin https://YOUR_TOKEN@github.com/LazizjanAsatov/study-planner.git

# Или используйте GitHub CLI
gh auth login
```

## Рекомендация

Используйте **SSH** - это безопаснее и удобнее:
- Не нужно вводить токен каждый раз
- Более безопасно
- Работает автоматически
