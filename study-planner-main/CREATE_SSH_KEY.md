# 🔑 Создание нового SSH ключа для GitHub

## Шаг 1: Создайте новый SSH ключ

Выполните в терминале:

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

**Важно**: Замените `your_email@example.com` на ваш email (можно использовать GitHub email).

Когда спросит:
- **"Enter file in which to save the key"**: Нажмите **Enter** (использует путь по умолчанию)
- **"Enter passphrase"**: Нажмите **Enter** (без пароля) или введите пароль для безопасности
- **"Enter same passphrase again"**: Повторите пароль или нажмите Enter

## Шаг 2: Скопируйте публичный ключ

```bash
cat ~/.ssh/id_ed25519.pub
```

Скопируйте весь вывод (начинается с `ssh-ed25519` и заканчивается вашим email).

## Шаг 3: Добавьте ключ в GitHub

1. Перейдите на: https://github.com/settings/keys
2. Нажмите **"New SSH key"**
3. **Title**: `MacBook Air` (или любое название)
4. **Key**: Вставьте скопированный ключ
5. Нажмите **"Add SSH key"**

## Шаг 4: Проверьте подключение

```bash
ssh -T git@github.com
```

Должно показать:
```
Hi LazizjanAsatov! You've successfully authenticated, but GitHub does not provide shell access.
```

## Шаг 5: Используйте SSH для Git

```bash
# Измените remote на SSH
git remote set-url origin git@github.com:LazizjanAsatov/study-planner.git

# Проверьте
git remote -v

# Загрузите код
git push -u origin main
```

## Готово! 🎉

Теперь вы можете использовать Git без пароля!
